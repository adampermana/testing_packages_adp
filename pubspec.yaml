name: testing_packages_adp
description: A Flutter plugin for creating and managing dynamic, multi-column tables with customizable features such as dropdowns, validation, read-only cells, and more. Ideal for applications that require interactive table inputs and structured data handling.
version: 0.0.1
homepage: https://github.com/adampermana/testing_packages_adp.git
issue_tracker: https://github.com/adampermana/testing_packages_adp/issues

environment:
  sdk: '>=3.1.5 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
#flutter:
